# Kafka 101

Assuming you downloaded the latest Kafka version, and launched it, simply produce messages using:

```bash
python Producer.py
```

Wait for the script to finish (or open another terminal tab), and consume the messages:

```bash
python Consumer.py
```
